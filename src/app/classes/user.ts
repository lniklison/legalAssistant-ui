import { Component, OnInit } from '@angular/core';

export class User {
  name: string;
  lastName: string;
  document: string;
  documentType: number;
  email: string;
  active: boolean;

  constructor() {
  }
}

